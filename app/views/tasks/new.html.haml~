%h2 Create New Movie

= form_tag tasks_path, :method => :post do

  = label :task, :name, 'Name'
  = select :task, :name, ['Inverse bottoms', 'circuit boarding', 'Mate cubes']

  = label :task, :quantity, 'Quantity'
  = text_field :task, :quantity
 
  = label :task, :notes, 'Notes'
  = text_field :task, :notes

  = submit_tag 'Save Changes'
